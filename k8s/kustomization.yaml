apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hanzo

resources:
  - kms-secrets.yaml
  - configmap.yaml
  - deployment.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/name: tasks
      app.kubernetes.io/part-of: hanzo-platform
      app.kubernetes.io/managed-by: hanzo-tasks
    includeSelectors: false
